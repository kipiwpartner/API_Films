<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobiles.exemple.gestionlivres_volley_json_h19">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ConfigApp"
        android:theme="@style/AppTheme">
        <activity android:name=".ModifierActivity"></activity>
        <activity android:name=".EnleverActivity" />
        <activity android:name=".ListerActivity" />
        <activity android:name=".EnregistrerActivity" />
        <activity android:name=".GestionLivresActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>



</manifest>